mypageです
<%if current_user.id == params[:id].to_i %>
  本人用のmypage
  idは<%= current_user.id%>です
  nameは<%= current_user.name %>
  birth_dayは<%= current_user.birth %>
  読了は、
  <% @user.end_books.each do |d| %>
    <% b = Book.find(d.book_id) %>
    <%= b.title %>
    <%= b.author %>
    <%= link_to book_path b.id  do %>
    <%= image_tag "#{b.id}.jpg" ,alt: "タイトル" %>
    <% end %>
    読了時期
    <%= d.created_at %>

    読了した感想
    <%= d.comments %>
  <% end %>
<% else %>
  他人閲覧用のmypage
  読了は、
  <% @user.end_books.each do |d| %>
    <% b = Book.find(d.book_id) %>
    <%= b.title %>
    <%= b.author %>
    <%= link_to user_user_book_path b.id  do %>
    <%= image_tag "#{b.id}.jpg" ,alt: "タイトル" %>
  <% end %>
    読了時期
    <%= d.created_at %>

  <% end %>
<% end %>