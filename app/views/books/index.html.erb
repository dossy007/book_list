
本の一覧

<% @books.each do |book| %>
  <div class="book">
  <a class="" href="<%= book_path book.id  %>">
    <%= image_tag "#{book.id}.jpg" ,alt: "タイトル" %>
  </a>
   title <%= book.title %>
   date <%= book.date %>
   author <%= book.author %>
   summary <%= book.summary %>
  </div>
<% end %>